# bn-webapp — agent guide

Read the monorepo guide first: `../AGENTS.md`. This file only adds package notes.

Vite + React companion UI at `app.better-net.com`. Sign-in required. Status: `status.md`.

```bash
npm install && npm run build && npm test
npm run dev   # Vite, proxies /api to local bn-server
```
