/** Click Unbait's tags (terminology.md). Offered in the Content Analysis modal's "+" select. */
import type { TagSpec } from '../../types/Tag.js';

export const CLICK_UNBAIT_TAGS: TagSpec[] = [
	{ id: 'clickbait', label: 'Clickbait headline' },
];
