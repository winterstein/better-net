
Specifications for privacy-shield feature

User can override per domain in Off-List.

Uses IAB TCF vendor list as one source to identify trackers.
The extension ships with a snapshot of the list. 
It periodically gets updates from the update server.

Stops tracking calls from web pages.

Settings module: `privacyShield`.

## Status

No content Tags (terminology.md). Works on scripts and web requests, not chunk labels.

Coverage overall: partial / prototype (update-manager snapshot path; request blocking not fully wired).
